services:
  agent-server:
    build:
      context: .
      dockerfile: Dockerfile.agent
    volumes:
      - agent-data:/data
    environment:
      SQLITE_PATH: /data/agent.db
    env_file:
      - ./agent/.env
    restart: unless-stopped

volumes:
  agent-data:
