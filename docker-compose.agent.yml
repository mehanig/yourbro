services:
  agent-server:
    build:
      context: .
      dockerfile: Dockerfile.agent
    volumes:
      - agent-data:/data
    environment:
      YOURBRO_SQLITE_PATH: /data/agent.db
    env_file:
      - ./agent/.env
    restart: unless-stopped

volumes:
  agent-data:
